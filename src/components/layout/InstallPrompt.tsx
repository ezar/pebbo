import { useEffect, useState } from 'react';import { Button } from '../ui/Button';
type BeforeInstallPromptEvent=Event&{prompt:()=>Promise<void>;userChoice:Promise<{outcome:string}>};
export const InstallPrompt=()=>{const [event,setEvent]=useState<BeforeInstallPromptEvent|null>(null);useEffect(()=>{const handler=(e:Event)=>{e.preventDefault();setEvent(e as BeforeInstallPromptEvent)};window.addEventListener('beforeinstallprompt',handler);return()=>window.removeEventListener('beforeinstallprompt',handler)},[]);if(!event)return null;return <aside className="fixed inset-x-4 bottom-4 mx-auto max-w-md rounded-3xl bg-white p-4 shadow-2xl"><p className="mb-3 font-bold">¿Instalar Pebbo para jugar sin conexión?</p><div className="flex gap-2"><Button onClick={async()=>{await event.prompt();setEvent(null)}}>Instalar</Button><Button variant="ghost" onClick={()=>setEvent(null)}>Luego</Button></div></aside>}
