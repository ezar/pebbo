import { describe, expect, it } from 'vitest';import { applyPetAction, clampStatus, createPet, evolvePet, getEmotionalState } from './petEngine';import { calculateElapsedTime } from './timeEngine';
describe('pet engine',()=>{const now='2026-07-12T00:00:00.000Z';it('calcula tiempo transcurrido con escala',()=>{expect(calculateElapsedTime(now,'2026-07-12T01:00:00.000Z',2).gameHours).toBe(2)});it('limita indicadores entre 0 y 100',()=>{expect(clampStatus({hunger:120,happiness:-2,hygiene:50,energy:99.6,health:0})).toEqual({hunger:100,happiness:0,hygiene:50,energy:100,health:0})});it('degrada con el tiempo',()=>{const pet=createPet('Luma','sprout','coral',now);const next=evolvePet(pet,'2026-07-12T01:00:00.000Z',1);expect(next.status.hunger).toBeLessThan(pet.status.hunger);expect(next.status.hygiene).toBeLessThan(pet.status.hygiene)});it('alimentar mejora hambre y salud',()=>{const pet={...createPet('Luma','sprout','coral',now),status:{hunger:20,happiness:50,hygiene:50,energy:50,health:50}};const next=applyPetAction(pet,'feed',now);expect(next.status.hunger).toBe(42);expect(next.status.health).toBe(53)});it('jugar mejora felicidad pero consume energía e higiene',()=>{const pet=createPet('Luma','sprout','coral',now);const next=applyPetAction(pet,'play',now);expect(next.status.happiness).toBeGreaterThan(pet.status.happiness);expect(next.status.energy).toBeLessThan(pet.status.energy);expect(next.status.hygiene).toBeLessThan(pet.status.hygiene)});it('limpiar mejora higiene',()=>{const pet={...createPet('Luma','sprout','coral',now),status:{hunger:50,happiness:50,hygiene:20,energy:50,health:50}};expect(applyPetAction(pet,'clean',now).status.hygiene).toBe(48)});it('dormir recupera energía con el tiempo',()=>{const pet={...createPet('Luma','sprout','coral',now),asleep:true,status:{hunger:50,happiness:50,hygiene:50,energy:20,health:50}};expect(evolvePet(pet,'2026-07-12T01:00:00.000Z',1).status.energy).toBeGreaterThan(20)});it('calcula estado emocional',()=>{const pet={...createPet('Luma','sprout','coral',now),status:{hunger:10,happiness:80,hygiene:80,energy:80,health:80}};expect(getEmotionalState(pet)).toBe('hungry')});});
