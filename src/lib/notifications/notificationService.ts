import { getNotificationSupport } from '../../engine/notificationEngine';
export interface NotificationService{isSupported():boolean;getPermission():NotificationPermission;requestPermission():Promise<NotificationPermission>;subscribe():Promise<PushSubscription|null>;unsubscribe():Promise<void>;sendTestNotification():Promise<void>}
const urlBase64ToUint8Array=(base64:string)=>{const padding='='.repeat((4-(base64.length%4))%4);const base64Safe=(base64+padding).replace(/-/g,'+').replace(/_/g,'/');const raw=atob(base64Safe);return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)));};
export const browserNotificationService:NotificationService={isSupported:()=>getNotificationSupport().supported,getPermission:()=>typeof Notification==='undefined'?'denied':Notification.permission,async requestPermission(){if(!this.isSupported())return 'denied';return Notification.requestPermission();},async subscribe(){if(!this.isSupported()||Notification.permission!=='granted')return null;const registration=await navigator.serviceWorker.ready;const existing=await registration.pushManager.getSubscription();if(existing)return existing;const key=import.meta.env.VITE_VAPID_PUBLIC_KEY as string|undefined;if(!key)return null;const sub=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:urlBase64ToUint8Array(key)});await fetch('/api/push/subscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({subscription:sub})});return sub;},async unsubscribe(){if(!('serviceWorker'in navigator))return;const reg=await navigator.serviceWorker.ready;const sub=await reg.pushManager.getSubscription();if(sub){await fetch('/api/push/unsubscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({endpoint:sub.endpoint})});await sub.unsubscribe();}},async sendTestNotification(){if(!this.isSupported()||Notification.permission!=='granted')return;const reg=await navigator.serviceWorker.ready;await reg.showNotification('Pebbo te saluda', {body:'Tu Pebbo está listo para recibir mimos.',icon:'/icons/pwa-192.svg',badge:'/icons/pwa-192.svg',data:{url:'/'}});}};
